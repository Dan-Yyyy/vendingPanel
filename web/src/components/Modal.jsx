import React from "react";
import styled from "styled-components";

const ModalContainer = styled.div`
  position: absolute;
`;

export const Modal = () => {
  return(
    <ModalContainer>
      Modal
    </ModalContainer>
  )
}